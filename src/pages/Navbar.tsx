import MenuBar from "@/components/ui/menubar";

const Navbar = () => {
  return (
    <div className="w-full py-6">
      <div className="max-w-10/12 flex items-center justify-center mx-auto">
        <MenuBar></MenuBar>
      </div>
    </div>
  );
};

export default Navbar;
